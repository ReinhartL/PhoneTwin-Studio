import {useEffect,useRef} from 'react';
import * as THREE from 'three';
const frag=`varying vec2 vUv;uniform vec2 uCanvas;uniform float uTime,uSpeed,uFrequency,uNoise,uIntensity,uBandWidth;uniform vec2 uRot;uniform vec3 uColors[3];void main(){vec2 p=vUv*2.-1.;vec2 q=vec2(p.x*uCanvas.x/uCanvas.y,p.y);float t=uTime*uSpeed;float a=atan(uRot.y,uRot.x);q=mat2(cos(a),-sin(a),sin(a),cos(a))*q+.25*cos(t);for(int i=0;i<3;i++)q+=sin(q.yx*uFrequency+cos(q*uFrequency+t))*.08;float v=0.;vec3 c=vec3(0.);for(int i=0;i<3;i++){float b=1.-smoothstep(0.,uBandWidth,abs(sin(q.x*1.3+q.y*.7+float(i)*2.1+t*.7)));c+=uColors[i]*b;v=max(v,b);}c*=uIntensity;float n=fract(sin(dot(gl_FragCoord.xy+uTime,vec2(12.9898,78.233)))*43758.5453);c+=(n-.5)*uNoise;gl_FragColor=vec4(clamp(c,0.,1.),.82);}`;
const vert=`varying vec2 vUv;void main(){vUv=uv;gl_Position=vec4(position,1.);}`;
export default function ColorBends({colors=['#0d2332','#0b5965','#65d5ff'],speed=.2,frequency=1,noise=.12,bandWidth=.55,rotation=90,intensity=1.1}){const ref=useRef();useEffect(()=>{const el=ref.current,scene=new THREE.Scene(),cam=new THREE.Camera(),renderer=new THREE.WebGLRenderer({antialias:false,alpha:true,preserveDrawingBuffer:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setClearColor(0,0);el.appendChild(renderer.domElement);const hex=h=>{const n=parseInt(h.replace('#',''),16);return new THREE.Vector3((n>>16&255)/255,(n>>8&255)/255,(n&255)/255)};const mat=new THREE.ShaderMaterial({vertexShader:vert,fragmentShader:frag,transparent:true,uniforms:{uCanvas:{value:new THREE.Vector2(1,1)},uTime:{value:0},uSpeed:{value:speed},uFrequency:{value:frequency},uNoise:{value:noise},uIntensity:{value:intensity},uBandWidth:{value:bandWidth},uRot:{value:new THREE.Vector2(Math.cos(rotation*Math.PI/180),Math.sin(rotation*Math.PI/180))},uColors:{value:colors.map(hex)}}});scene.add(new THREE.Mesh(new THREE.PlaneGeometry(2,2),mat));const clock=new THREE.Clock(),resize=()=>{renderer.setSize(el.clientWidth,el.clientHeight,false);mat.uniforms.uCanvas.value.set(el.clientWidth,el.clientHeight)};resize();addEventListener('resize',resize);let raf;const loop=()=>{mat.uniforms.uTime.value=clock.getElapsedTime();renderer.render(scene,cam);raf=requestAnimationFrame(loop)};loop();return()=>{cancelAnimationFrame(raf);removeEventListener('resize',resize);mat.dispose();renderer.dispose();el.removeChild(renderer.domElement)}},[colors,bandWidth,frequency,intensity,noise,rotation,speed]);return <div ref={ref} className="color-bends-bg"/>}
