# Model references

The procedural model was developed from external product reference imagery.
Those bitmap references and derived inspection crops are intentionally excluded
from the public repository because they are not covered by this project's MIT
License.

The runtime application does not require these images. To repeat the modeling
workflow, place your own licensed reference in `reference/` and any inspection
crops in `detail-grid/`.
