# PhoneTwinSender target

This directory contains the Swift sources and entitlements used by the main iOS application and shared with the ReplayKit extension.

Use the native development endpoint:

```text
ws://<MAC_IP>:8788/native
```

See the project root [README](../../README.md#打包并安装-ios-app) for signing, installation, permission, and broadcast instructions.
